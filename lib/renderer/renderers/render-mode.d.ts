export declare function isRenderOnceLayer(layer: {
    renderMode?: string;
}): boolean;
export declare function isRenderPrefetchLayer(layer: {
    renderMode?: string;
}): boolean;
export declare function isRenderDelayLayer(layer: {
    renderMode?: string;
}): boolean;
export declare function isRenderLayer(layer: {
    renderMode?: string;
}): boolean;
