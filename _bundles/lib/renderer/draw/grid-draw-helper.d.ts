import { Grid } from '../commons/grid/grid';
import { TilesPicture } from '../tiles/tiles-picture';
export declare class GridDrawHelper {
    static createCanvasFromGrid(grid: Grid, tilesPicture: TilesPicture): HTMLCanvasElement;
}
